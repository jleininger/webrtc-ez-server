version: "3.9"

services:
  signal-server:
    container_name: signal-server
    image: deno
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
      target: base
    ports:
      - "${PORT}:${PORT}"
