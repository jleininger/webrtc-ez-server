export enum MessageType {
  CONNECTED,
  JOIN_LOBBY,
  LEAVE_LOBBY,
  REMOVE_PLAYER,
  LOBBY_JOINED,
  LOBBY_LEFT,
  PEER_CONNECTED,
  PEER_DISCONNECTED,
  OFFER,
  ANSWER,
  CANDIDATE,
  ERROR,
  PING
}
